Workflow preference
===================

- Unless explicitly requested in the current turn, do not auto-run refresh scripts; provide commands for manual run instead.
- Keep `Chat` / `Files` / `Diff` workspace tabs on the same split behavior pattern across resolutions.
