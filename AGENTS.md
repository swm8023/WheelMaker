Preflight (mandatory, every new session):
1. Read `CLAUDE.md` at repo root first.
2. Then choose one track:
   - Go daemon work: read `server/AGENTS.md`
   - Flutter app work: read `app/AGENTS.md`
3. Before doing any code/task action, explicitly confirm:
   `READ_OK: CLAUDE.md + <track>/AGENTS.md`

Do not skip the above even if context seems sufficient.

