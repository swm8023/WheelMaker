# WheelMaker ???????????(V1 ??)

## 1. ?????

?????????????? WheelMaker ????,????:

- ????????:
  - ??????
  - ?????????
  - ???????????
- Git ????:
  - ????
  - ????????
  - ??????????
  - ???????? diff

??:

- ?? `client`(project)????????
- ?? IM ????,??????? WheelMaker ?????
- ?????? WheelMaker(????)?
- ??????????,?????????
- ???????(???? App ? Web)?
- ??????????????

## 2. ????

### 2.1 ??

- `WheelMaker (Registry Server thread, Remote Observe Mode)`
  - ??????
  - ?? TCP ??(???? TLS)
  - ????????
- `Project Runtime (existing client model)`
  - ????????? client ???
  - ????/Git????
- `Remote Client`
  - ??????????
  - ????????,??? UI ????

### 2.2 ????

- ???????:`WebSocket + JSON`
- ???????? project(?? `projectId` ????)
- ??-????,??????(`requestId` ??)
- ???????(V1 ????,?????)

## 3. ????

- ??????:????? UI ??????
- ????:??????????????/???
- ?????:????????(? `project.list`?`project.listFull`)??????,??????? `projectId`?
- ????:V1 ???????
- ???:?????? `version`,????????????

## 4. ???????

### 4.1 ??????

```json
{
  "version": "1.0",
  "requestId": "uuid-or-seq",
  "type": "request|response|error|event",
  "method": "fs.list|git.log|...",
  "projectId": "project-name",
  "payload": {}
}
```

### 4.2 ????

```json
{
  "version": "1.0",
  "requestId": "same-as-request",
  "type": "error",
  "error": {
    "code": "UNAUTHORIZED|NOT_FOUND|INVALID_ARGUMENT|INTERNAL|RATE_LIMITED",
    "message": "human readable",
    "details": {}
  }
}
```

## 5. ?????(V1)

### 5.1 `hello`

??:

```json
{
  "type": "request",
  "method": "hello",
  "payload": {
    "clientName": "wm-web",
    "clientVersion": "0.1.0",
    "protocolVersion": "1.0"
  }
}
```

??:

```json
{
  "type": "response",
  "method": "hello",
  "payload": {
    "serverVersion": "x.y.z",
    "protocolVersion": "1.0",
    "features": {
      "fs": true,
      "git": true,
      "push": false
    }
  }
}
```

### 5.2 `auth`(??)

- V1 ??????/? token ???
- ???? mTLS / OAuth2 / ???? token?

### 5.3 ??????

### 5.3.1 ????:`project.list`

??:

```json
{
  "type": "request",
  "method": "project.list",
  "payload": {}
}
```

??:

```json
{
  "type": "response",
  "method": "project.list",
  "payload": {
    "projects": [
      { "projectId": "server", "name": "server", "online": true },
      { "projectId": "app", "name": "app", "online": true }
    ]
  }
}
```

### 5.3.2 ???? project ????:`project.listFull`

????????? project ???????????,???????????????????

??:

```json
{
  "type": "request",
  "method": "project.listFull",
  "payload": {
    "includeStats": true
  }
}
```

??:

```json
{
  "type": "response",
  "method": "project.listFull",
  "payload": {
    "projects": [
      {
        "projectId": "server",
        "name": "server",
        "cwd": "D:/Code/WheelMaker/server",
        "online": true,
        "capabilities": { "fs": true, "git": true },
        "git": { "currentBranch": "main" },
        "stats": { "lastActiveAt": "2026-03-26T09:20:00Z" }
      },
      {
        "projectId": "app",
        "name": "app",
        "cwd": "D:/Code/WheelMaker/app",
        "online": true,
        "capabilities": { "fs": true, "git": true },
        "git": { "currentBranch": "main" },
        "stats": { "lastActiveAt": "2026-03-26T09:10:00Z" }
      }
    ]
  }
}
```

??:

- `project.listFull` ?????,?????? `projectId`?
- `includeStats` ??,?? `false`;? `true` ???????????(? `lastActiveAt`)?
- ???? `cwd` ????????????

## 6. ??????(??)

### 6.1 ???:`fs.list`

??:

```json
{
  "type": "request",
  "method": "fs.list",
  "projectId": "server",
  "payload": {
    "path": ".",
    "cursor": "",
    "limit": 200
  }
}
```

??:

```json
{
  "type": "response",
  "method": "fs.list",
  "projectId": "server",
  "payload": {
    "path": ".",
    "entries": [
      { "name": "cmd", "path": "cmd", "kind": "dir", "size": 0, "mtime": "..." },
      { "name": "go.mod", "path": "go.mod", "kind": "file", "size": 245, "mtime": "..." }
    ],
    "nextCursor": ""
  }
}
```

??:

- ????????,???? `fs.list(path=<clickedDir>)`?
- ??????,???????????

### 6.2 ???:`fs.read`

??:

```json
{
  "type": "request",
  "method": "fs.read",
  "projectId": "server",
  "payload": {
    "path": "internal/agent/codex/agent.go",
    "offset": 0,
    "limit": 65536
  }
}
```

??:

```json
{
  "type": "response",
  "method": "fs.read",
  "projectId": "server",
  "payload": {
    "path": "internal/agent/codex/agent.go",
    "content": "...",
    "encoding": "utf-8",
    "eof": true,
    "nextOffset": 65536
  }
}
```

## 7. Git ????(??)

### 7.1 ????:`git.branches`

??:

```json
{
  "type": "request",
  "method": "git.branches",
  "projectId": "server",
  "payload": {}
}
```

??:

```json
{
  "type": "response",
  "method": "git.branches",
  "projectId": "server",
  "payload": {
    "current": "main",
    "branches": ["main", "feature/x"]
  }
}
```

### 7.2 ????:`git.log`

??:

```json
{
  "type": "request",
  "method": "git.log",
  "projectId": "server",
  "payload": {
    "ref": "main",
    "cursor": "",
    "limit": 50
  }
}
```

??:

```json
{
  "type": "response",
  "method": "git.log",
  "projectId": "server",
  "payload": {
    "ref": "main",
    "commits": [
      {
        "sha": "abc123",
        "author": "name",
        "email": "x@y.z",
        "time": "...",
        "title": "commit subject"
      }
    ],
    "nextCursor": "opaque-cursor"
  }
}
```

### 7.3 ??????:`git.commit.files`

??:

```json
{
  "type": "request",
  "method": "git.commit.files",
  "projectId": "server",
  "payload": {
    "sha": "abc123"
  }
}
```

??:

```json
{
  "type": "response",
  "method": "git.commit.files",
  "projectId": "server",
  "payload": {
    "sha": "abc123",
    "files": [
      { "path": "internal/im/feishu/feishu.go", "status": "M", "additions": 10, "deletions": 2 }
    ]
  }
}
```

### 7.4 ??? diff:`git.commit.fileDiff`

??:

```json
{
  "type": "request",
  "method": "git.commit.fileDiff",
  "projectId": "server",
  "payload": {
    "sha": "abc123",
    "path": "internal/im/feishu/feishu.go",
    "contextLines": 3
  }
}
```

??:

```json
{
  "type": "response",
  "method": "git.commit.fileDiff",
  "projectId": "server",
  "payload": {
    "sha": "abc123",
    "path": "internal/im/feishu/feishu.go",
    "isBinary": false,
    "diff": "@@ ...",
    "truncated": false
  }
}
```

## 8. ?????????

- ? `projectId` ????? client/project ???(cwd?git repo)?
- ????? git ????????????
- ????????(? 5s/10s)?
- ?????(? diff / ????)????????? `truncated=true`?

## 9. ????(V1 ???)

### 9.1 ?????

- ?? `auth` ??(token/mTLS/OAuth2)?
- ??????????(??? project ???????)?
- project ? ACL:??????????

### 9.2 ????

- ?? TLS(???? TLS)?
- ??????????????

### 9.3 ????

- ??????? project root ?,??????
- ????????(??? denylist)?
- Git diff/???????????(???????)?

### 9.4 ?????

- ????????:???projectId?method????????
- ???????????
- ???????,???????

## 10. ??????

- `version` ?????????
- ??????????(?????????)?
- ?????? `2.x` ?????????

## 11. V1 ??????

1. ???:WebSocket + `hello` + ??????
2. ????:`project.list` + `project.listFull`
3. ????:`fs.list`?`fs.read`
4. Git??:`git.branches`?`git.log`?`git.commit.files`?`git.commit.fileDiff`
5. ?????:token + root ?? + ????
6. ????:??/????????????

---

??????? V1,?????????????,??? App/Web ????

